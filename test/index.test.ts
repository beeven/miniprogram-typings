// 测试扩展wxNS方法

declare namespace wx {
  interface Wx {
    /**
     * add test method
     */
    test(): any;
  }
}

wx.test();

wx.test()

wx.authorize({
  scope: 'scope.record',
})
getApp().asdf;

getCurrentPages()[0].data;